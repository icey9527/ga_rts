-- 通讯节奏配置。修改这里即可调整同一机体连续发言的最短间隔。
return {
    report_interval = 0.30,
    formation_follow_continue_interval = 20.0,
    camera_follow_continue_delay = 10.0,
    camera_follow_continue_interval = 20.0,
    camera_follow_start_interval = 2.5,
    camera_follow_end_enabled = true,
    camera_follow_end_min_duration = 10.0,
    follow_priority_window = 0.40,
}
